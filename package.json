{
  "name": "@xenoterracide/share",
  "packageManager": "yarn@4.18.0",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.2",
    "git-conventional-commits": "^2.8.0",
    "prettier": "^3.6.2",
    "prettier-plugin-java": "^2.7.5",
    "prettier-plugin-properties": "^0.3.0",
    "prettier-plugin-toml": "^2.0.6"
  },
  "scripts": {
    "check": "yarn lint:prettier",
    "contribute": "uv sync --frozen && uv run --frozen pre-commit install --hook-type commit-msg --hook-type pre-commit --hook-type post-checkout --hook-type post-merge",
    "lint:prettier": "prettier --ignore-unknown --cache --check '**'",
    "lint:reuse": "uv run --frozen --group dev reuse lint",
    "lint": "yarn lint:prettier && yarn lint:reuse"
  }
}
