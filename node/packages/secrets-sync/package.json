{
  "name": "secrets-sync",
  "license": "GPL-3.0-or-later",
  "packageManager": "yarn@4.12.0",
  "devDependencies": {
    "@types/node": "^24.12.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  },
  "scripts": {
    "secrets-sync": "tsx secrets-sync.ts",
    "test": "vitest run && yarn typecheck",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit secrets-sync.ts"
  }
}
